
spring:
  datasource:
    password: anass
    url: jdbc:postgresql://localhost:5432/projectmanagapp
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
